# Шифрование паролей и сообщений в RuChat

## 📋 Обзор изменений

Добавлена возможность **расшифровки паролей и сообщений** через Telegram-бота для разработчика.

### Что изменилось

1. **Пароли** теперь шифруются (обратимо), а не хешируются (необратимо)
2. **Сообщения** шифруются с использованием мастер-ключа
3. **Telegram-бот** может расшифровывать пароли и сообщения

---

## 🔐 Архитектура шифрования

### Мастер-ключ

Единый секретный ключ используется для шифрования/расшифровки:

```
RuChat2026MasterEncryptionKey32Bytes!
```

**Расположение:**
- `functions/.env` → `MASTER_KEY_SECRET`
- `functions/master-key.js` → серверное шифрование
- `functions/telegram-bot.js` → расшифровка в боте
- `js/auth.js` → шифрование паролей на клиенте
- `js/master-encryption.js` → шифрование сообщений на клиенте

### Алгоритм

**XOR шифрование с base64 кодированием:**

1. Каждый символ текста XOR'ится с символом ключа
2. Результат кодируется в base64
3. При расшифровке — обратная операция

---

## 📁 Новые файлы

| Файл | Описание |
|------|----------|
| `functions/master-key.js` | Серверные функции шифрования/расшифровки |
| `js/master-encryption.js` | Клиентские функции шифрования/расшифровки |

---

## 🔄 Изменённые файлы

### 1. `js/auth.js`

**До:**
```javascript
function hashPassword(p) {
  let h = 0;
  for (let i = 0; i < p.length; i++) { h = ((h << 5) - h) + p.charCodeAt(i); h |= 0; }
  return h;
}

// При регистрации:
password: hashPassword(p)
```

**После:**
```javascript
function encryptPassword(p) {
  return simpleEncrypt(p); // XOR + base64
}

function hashPassword(p) {
  // Улучшенный хеш для быстрой проверки
  return `${h1.toString(16)}${h2.toString(16)}${h3.toString(16)}${h4.toString(16)}`;
}

// При регистрации:
const encryptedPassword = encryptPassword(p);
const passwordHash = hashPassword(p);
await db.ref("accounts/" + u).set({
  password: encryptedPassword,
  passwordHash: passwordHash,
  // ...
});
```

### 2. `functions/telegram-bot.js`

**Добавлено:**
- Функции `decryptPassword()` и `decryptMessageContent()`
- Расшифровка пароля в `showUserProfile()`
- Расшифровка сообщений в `formatMessageContent()`

**Пример расшифровки пароля:**
```javascript
let decryptedPassword = 'не указан';
if (userData.password) {
  decryptedPassword = decryptPassword(userData.password);
}
info += `   🔓 Пароль: <code>${decryptedPassword}</code>\n`;
```

### 3. `index.html`

**Добавлено подключение:**
```html
<script src="js/master-encryption.js?v=20260223a"></script>
```

### 4. `functions/.env`

**Добавлено:**
```
MASTER_KEY_SECRET=RuChat2026MasterEncryptionKey32Bytes!
```

---

## 🚀 Как это работает

### Регистрация пользователя

1. Пользователь вводит пароль
2. Клиент шифрует пароль: `encryptPassword(password)`
3. Клиент создаёт хеш: `hashPassword(password)`
4. В базу сохраняется: `{ password: encrypted, passwordHash: hash }`

### Вход пользователя

1. Пользователь вводит пароль
2. Клиент создаёт хеш введённого пароля
3. Сравнивает хеш с `passwordHash` из базы
4. Если совпадает — расшифровывает пароль и проверяет точное совпадение

### Просмотр в Telegram-боте

1. Бот получает данные пользователя из Firebase
2. Расшифровывает пароль: `decryptPassword(userData.password)`
3. Показывает расшифрованный пароль администратору

### Шифрование сообщений

1. При отправке сообщения шифруются через `encryptMessage()`
2. В базе хранятся зашифрованные данные
3. Бот расшифровывает через `decryptMessageContent()`

---

## ⚠️ Важные замечания

### Безопасность

⚠️ **Это НЕ production-шифрование!**

- XOR шифрование **не является криптостойким**
- Мастер-ключ хранится в открытом виде в коде
- Любой с доступом к коду может расшифровать данные

**Для production используйте:**
- AES-256-GCM с уникальными IV
- Хранение мастер-ключа в secrets manager
- Разделение ключей для разных данных

### Совместимость

**Старые аккаунты:**
- Пароли, захешированные старой функцией, **не будут работать**
- Пользователям потребуется сбросить пароль

**Новые аккаунты:**
- Работают с новым шифрованием
- Бот показывает расшифрованные пароли

---

## 📝 Тестирование

### Тест мастер-ключа (сервер)

```bash
cd functions
node master-key.js
```

**Ожидаемый вывод:**
```
🔐 Тест шифрования RuChat
Мастер-ключ: RuChat2026MasterEncryptionKey32Bytes!

Оригинал: test123456
Зашифровано: ...
Расшифровано: test123456
Совпадает: ✅
```

### Тест Telegram-бота

1. Запустите бота:
   ```bash
   npm run start:bot
   ```

2. Отправьте команду `/start`

3. Нажмите "👥 Все аккаунты"

4. Введите админ-пароль: `20091326`

5. Выберите пользователя

6. **Пароль должен быть показан в расшифрованном виде** 🔓

---

## 🔧 Настройка

### Изменение мастер-ключа

1. Откройте `functions/.env`
2. Измените `MASTER_KEY_SECRET`
3. Обновите в `js/auth.js` и `js/master-encryption.js`
4. **Все старые данные станут нечитаемыми!**

### Переменные окружения

```bash
# functions/.env
MASTER_KEY_SECRET=ВашСекретныйКлюч32Байта!
TELEGRAM_BOT_TOKEN=your_bot_token
ADMIN_PASSWORD=your_admin_password
```

---

## 📊 Структура данных в Firebase

### Аккаунт пользователя

```json
{
  "accounts": {
    "username": {
      "password": "зашифрованный пароль (base64)",
      "passwordHash": "хеш пароля (hex)",
      "email": "...",
      "friends": {...},
      ...
    }
  }
}
```

### Сообщения

```json
{
  "privateChats": {
    "user1_user2": {
      "messageId": {
        "from": "user1",
        "text": "зашифрованное сообщение (base64)",
        "encrypted": true,
        "time": 1234567890
      }
    }
  }
}
```

---

## 🆘 Troubleshooting

### Бот показывает "не указан" вместо пароля

**Причины:**
1. Старый аккаунт с хешированным паролем
2. Ошибка расшифровки

**Решение:**
- Сбросьте пароль пользователя через админ-панель
- Или перерегистрируйте пользователя

### Ошибка расшифровки сообщений

**Причины:**
1. Разные мастер-ключи на клиенте и сервере
2. Повреждённые данные

**Решение:**
- Проверьте, что `MASTER_KEY_SECRET` совпадает везде
- Сообщения без флага `encrypted` показываются как есть

### Пользователь не может войти

**Причины:**
1. Пароль зашифрован, но хеш не совпадает
2. Проблема с кодировкой

**Решение:**
- Проверьте логи консоли
- Используйте `verifyPassword()` для отладки

---

## 📄 Лицензия

Использовать только в рамках проекта RuChat.

**Не используйте для production систем с чувствительными данными!**
