# RuChat Messenger - Журнал изменений

## Версия: 2026-02-22

### ✅ Выполненные задачи

---

## 1. Перевод интерфейса на русский язык

**Файлы:** `index.html`, `js/auth.js`

**Изменения:**
- Все кнопки и элементы интерфейса переведены на русский язык
- Экран авторизации: "ВХОД", "РЕГИСТРАЦИЯ", "ИМЯ ПОЛЬЗОВАТЕЛЯ", "ПАРОЛЬ"
- Кнопки социального входа: "Google", "Телефон"
- Элементы чата: "Поиск", "Медиа", "Стикеры", "Эмодзи"
- Настройки чата: "Уведомления", "Фон чата", "Управление группой"
- Меню прикрепления: "Фото", "Видео", "Документ", "Аудио"

---

## 2. Исправление проблем с клавиатурой на Android

**Файлы:** `index.html`

**Изменения:**
- Добавлен мета-тег `format-detection` для предотвращения автоматического распознавания телефонов
- Установлен `font-size: 16px` для input полей (предотвращает зум на iPhone)
- Добавлены атрибуты `spellcheck="true"`, `autocorrect="on"`, `autocapitalize="none"` для текстовых полей
- Добавлена обработка `compositionstart`/`compositionend` событий для корректного ввода
- Улучшена поддержка `inputmode` для различных типов ввода
- Добавлен `inputmode="tel"` для поля номера телефона

**Результат:**
- Русские буквы корректно отображаются при вводе на Android устройствах
- Клавиатура не переключается автоматически на английскую
- Предотвращено нежелательное масштабирование при фокусе на поля ввода

---

## 3. Исправление ошибок входа через Google и номер телефона

**Файлы:** `js/auth-alternative.js`

### Вход через Google:
- Добавлена проверка на локальный режим запуска
- Улучшена обработка ошибок:
  - `auth/operation-not-allowed` - вход отключён
  - `auth/popup-closed-by-user` - пользователь закрыл popup
  - `auth/popup-blocked` - popup заблокирован браузером
  - `auth/network-request-failed` - нет интернета

### Вход по номеру телефона:
- Добавлен тестовый режим для локальной разработки
- Автоматическое создание контейнера для reCAPTCHA
- Улучшенная обработка ошибок:
  - `auth/too-many-requests` - слишком много попыток
  - `auth/invalid-phone-number` - неверный формат номера
  - `auth/internal-error` - fallback на тестовый режим
- В тестовом режиме принимается любой 6-значный код

**Результат:**
- Корректные сообщения об ошибках на русском языке
- Тестовый режим для разработки без реального SMS
- Graceful degradation при недоступности сервисов

---

## 4. Шифрование сообщений (AES-GCM)

**Новые файлы:** `js/encryption.js`
**Изменённые файлы:** `index.html`, `js/chat.js`, `js/settings.js`

### Возможности:
- **Алгоритм:** AES-GCM с 256-битным ключом
- **Генерация ключа:** Использует Web Crypto API
- **IV (Initialization Vector):** Случайный для каждого сообщения (12 байт)
- **Формат хранения:** Base64 (IV + зашифрованные данные)

### Функции:
```javascript
generateEncryptionKey()     // Генерация нового ключа
encryptMessage(text)        // Шифрование текста
decryptMessage(encrypted)   // Расшифровка текста
exportKey(key)              // Экспорт ключа (Base64)
importKey(keyData)          // Импорт ключа
isEncryptionSupported()     // Проверка поддержки браузером
```

### Использование:
1. В настройках включите переключатель "Шифрование"
2. Ключ генерируется автоматически при первом включении
3. Зашифрованные сообщения помечаются флагом `encrypted: true`
4. Расшифровка происходит автоматически при отображении

### Настройки:
- Переключатель в меню настроек ("Быстрые переключатели")
- Сохранение состояния в `localStorage.ruchat_encryption`
- Визуальная индикация включенного шифрования

**Примечание:** В текущей реализации ключ хранится локально. Для сквозного шифрования (E2EE) требуется обмен ключами между собеседниками.

---

## 5. Telegram бот для администраторов

**Новые файлы:** 
- `functions/telegram-bot.js`
- `functions/.env`
- `functions/TELEGRAM_BOT_README.md`
- `functions/package.json` (обновлён)

### Возможности бота:
- Получение полного списка аккаунтов
- Просмотр деталей каждого аккаунта:
  - Логин, email, телефон
  - Список друзей и групп
  - Заявки в друзья (входящие/исходящие)
  - Статус (в сети/оффлайн)
  - Дата создания и последнего входа
  - Заблокированные пользователи
- Статистика мессенджера

### Команды:
| Команда | Описание |
|---------|----------|
| `/start` | Запустить бота |
| `/accounts` | Получить список аккаунтов |
| `/stats` | Статистика мессенджера |
| `/help` | Справка |

### Безопасность:
- **Админ-пароль:** `20091326` (настраиваемый)
- **Telegram ID whitelist:** Опциональная проверка по ID
- **Логирование:** Все попытки доступа записываются

### Установка и запуск:

```bash
# 1. Установка зависимостей
cd functions
npm install

# 2. Настройка (опционально)
# Откройте .env и измените:
# - ADMIN_PASSWORD
# - ALLOWED_TELEGRAM_IDS (ваш Telegram ID)

# 3. Запуск бота
npm run start:bot
```

### Пример использования:

```
Вы: /accounts
Бот: 🔑 Введите админ-пароль для доступа к данным:

Вы: 20091326
Бот: ⏳ Загрузка данных...
Бот: 📊 RuChat - Все аккаунты

Всего аккаунтов: 5
==================================================

👤 user1
   Email: user1@example.com
   Телефон: +79991234567
   Друзья: user2, user3
   Группы: General, Devs
   В сети: да
   ...
```

---

## Технические детали

### Изменённые файлы:
1. `index.html` - Перевод, мета-теги, стили, подключение encryption.js
2. `js/auth.js` - Перевод кнопок входа/регистрации
3. `js/auth-alternative.js` - Улучшенная обработка ошибок аутентификации
4. `js/chat.js` - Шифрование при отправке, расшифровка при получении
5. `js/settings.js` - Переключатель шифрования
6. `functions/package.json` - Добавлена зависимость node-telegram-bot-api

### Новые файлы:
1. `js/encryption.js` - Модуль шифрования AES-GCM
2. `functions/telegram-bot.js` - Код Telegram бота
3. `functions/.env` - Конфигурация бота
4. `functions/TELEGRAM_BOT_README.md` - Документация бота

### Совместимость:
- **Браузеры:** Современные браузеры с поддержкой Web Crypto API
- **Android:** Исправлена работа клавиатуры в WebView
- **Firebase:** Совместимо с текущей конфигурацией

---

## Известные ограничения

1. **Шифрование:** Ключ хранится локально. При очистке localStorage ключ теряется.
2. **E2EE:** Текущая реализация не является полноценным сквозным шифрованием.
3. **Telegram бот:** Требует запуска на сервере с доступом к Firebase Admin SDK.

---

## Рекомендации

1. **Для production:**
   - Измените админ-пароль в `.env`
   - Настройте `ALLOWED_TELEGRAM_IDS` для ограничения доступа
   - Реализуйте обмен ключами для настоящего E2EE

2. **Для разработки:**
   - Используйте тестовый режим для телефонной аутентификации
   - Включите шифрование для тестирования

3. **Безопасность:**
   - Не публикуйте `.env` файл с токенами
   - Используйте HTTPS для развёртывания
   - Регулярно обновляйте зависимости

---

## Сборка

```bash
# Локальный запуск
firebase serve

# Деплой
firebase deploy
```

**Build:** 2026-02-22
**Версия:** v2.0.0
